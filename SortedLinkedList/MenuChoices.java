package Session5.SortedLinkedList;

/**
 * To add the menu choice enums.
 * Assumption : EMPTY_OPTION is intentionally added so choices can be matched starting from 1st.
 * @author Noor
 */
public enum MenuChoices {
	EMPTY_OPTION, INSERT, DISPLAY, EXIT
}
