package Session5.QueueImplementor;

/**
 * To add the menu choice enums.
 * Assumption : EMPTY_OPTION is intentionally added so the choice can be matched starting from 1st.
 * @author Noor
 */
public enum MenuChoice {
	EMPTY_OPTION, ENQUEUE, DEQUEUE, DISPLAY, EXIT
}
